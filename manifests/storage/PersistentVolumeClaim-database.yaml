apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ssd-nfs-pvc
  namespace: database
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: manual
  volumeName: ssd-nfs-pva
